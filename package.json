{
  "name": "werbesitztwas",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "check:sources": "node scripts/check-sources.mjs",
    "prebuild": "npm run check:sources && npm run clean:cache",
    "build": "astro build",
    "preview": "astro preview",
    "clean:cache": "node scripts/clear-cache.mjs"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.2",
    "astro": "^7.2.4",
    "js-yaml": "^4.1.0"
  }
}
